from setuptools import setup

PLUGIN_NAME="cm_plugin_name"

setup(
    name=PLUGIN_NAME,
    packages=[PLUGIN_NAME],
    version="0.0.1",
    description="Example how a plugin should look",
    url="www.example.com",
)

